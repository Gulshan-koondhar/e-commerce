import React from "react";

const Profile = () => {
  return (
    <div className="ring-1 ring-black rounded-md p-2 flex flex-col gap-2 absolute z-50 bg-white right-0">
      <button>Profile</button>
      <button>Logout</button>
    </div>
  );
};

export default Profile;
